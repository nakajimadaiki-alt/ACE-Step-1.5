# ACE-Step-1.5 使い方ガイド（BGM・動画生成機能）

このドキュメントでは、ACE-Stepを使用して音楽（Lofi Mix）およびそれに合わせたBGM動画を生成する手順を説明します。

## 1. 起動方法

UIを操作するためには、「APIサーバー（AIエンジン）」と「Web UI（操作画面）」の2つを起動する必要があります。

### 手順 1-1. APIサーバーの起動

音楽を実際に生成するための裏側（バックエンド）のサーバーです。

1. ターミナル（コマンドプロンプトやPowerShell）を新しく開きます。
2. `ACE-Step-1.5` フォルダに移動します。
3. 以下のコマンドを実行します。

   ```bat
   launchers\start_api_server.bat
   ```

4. 黒い画面に文字が流れ、`Uvicorn running on http://127.0.0.1:8001` のような表示が出れば起動完了です。（**※この画面は開いたまま閉じるまで放置してください**）

### 手順 1-2. BGMジェネレーターUIの起動

ブラウザで操作するための画面（フロントエンド）です。

1. **もう一つ新しいターミナル**を開きます。
2. 同様に `ACE-Step-1.5` フォルダに移動し、以下のコマンドを実行します。

   ```bat
   .venv\Scripts\python.exe app\bgm_generator_ui.py
   ```

3. `Running on local URL:  http://127.0.0.1:7860` と表示されたら準備完了です。

---

## 2. 音楽・動画の生成手順

UIが起動したら、ブラウザ（ChromeやEdgeなど）を開き、アドレスバーに **`http://127.0.0.1:7860`** と入力してアクセスします。

### 手順 2-1. 音楽の生成

最初は「動画」ではなく「音楽単体」を生成します。

1. 画面上部のスライダーで**「生成する曲数」**と**「1曲の長さ (秒)」**を設定します。
   - 例: 10曲・180秒
2. そのすぐ下にある **「✨ 音楽を生成する」** ボタンをクリックします。
3. 処理が始まります。曲数と長さによっては数分〜数十分かかる場合があります。
4. 画面下部のログボックスに `✅ 音楽生成完了！ 生成されたファイル: ...` と表示され、「生成された音楽ファイル」のプルダウンに新しく作られたファイルがセットされれば成功です。

> **⚠️ 重要:** 「曲数」や「秒数」の数値を変更した場合は、**必ずこの「✨ 音楽を生成する」ボタンを押し直して**ください。これを忘れて一番下の「🚀 動画を生成する」ボタンを押してしまうと、過去に作られた古い音楽データがそのまま使われてしまいます。

### 手順 2-2. 動画の生成

出来上がった音楽に映像（アニメーション）を付けてMP4動画を作成します。

1. 「動画モード」を選択します（例: ループ動画、回転ディスク、静止画）。
   - 必要に応じて、使用する動画素材（mp4など）や画像ファイル（jpg/pngなど）をアップロードまたはパスを指定してください。
2. 画面一番下の **「🚀 動画を生成する」** ボタンをクリックします。
3. しばらく待つと、音声と映像が合成されたMP4ファイルが完成し、UI上に動画プレイヤーが表示されます。
4. プレイヤーの右上などからダウンロードするか、プロジェクト内の `bgm_output` フォルダ内に保存されたmp4ファイルを直接確認してください。
